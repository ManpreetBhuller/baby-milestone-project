services:
  postgres:
    image: 'postgres:16'
    environment:
      - 'POSTGRES_DB=babysteps'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
    volumes:
      - 'babysteps-pgdata:/var/lib/postgresql/data'

volumes:
  babysteps-pgdata:
